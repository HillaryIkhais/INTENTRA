services:
  - type: web
    name: intentra-api
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node dist/live-api.js
    envVars:
      - key: NODE_ENV
        value: production
